{
  "name": "rpg-frontend-static",
  "version": "1.0.0",
  "description": "Frontend estático para el sistema RPG",
  "scripts": {
    "start": "http-server public -p $PORT --proxy http://localhost:$PORT?"
  },
  "dependencies": {
    "http-server": "^14.1.1"
  },
  "engines": {
    "node": ">=18.x"
  }
}